<div class="text-center mb-12">
  <h1 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl">
    Pay Gem + Stripe Demo
  </h1>
  <p class="mt-4 text-lg text-gray-600">
    An educational app for understanding the Pay gem with Stripe.
    <br>
    Subscribe to a software license plan, then explore the database and webhook data.
  </p>
</div>

<% if @subscription&.active? %>
  <div class="rounded-lg bg-green-50 border border-green-200 p-6 mb-8 text-center">
    <h2 class="text-xl font-semibold text-green-800">You have an active subscription!</h2>
    <p class="mt-2 text-green-700">
      Plan: <strong><%= @subscription.processor_plan %></strong> &middot;
      Status: <strong><%= @subscription.status %></strong>
    </p>
    <div class="mt-4 flex justify-center gap-4">
      <%= link_to "View Account", account_path, class: "rounded-md bg-green-600 px-4 py-2 text-sm font-semibold text-white hover:bg-green-500" %>
      <%= link_to "Explore Data", explorer_path, class: "rounded-md bg-indigo-600 px-4 py-2 text-sm font-semibold text-white hover:bg-indigo-500" %>
      <%= link_to "Lifecycle Demo", lifecycle_path, class: "rounded-md bg-purple-600 px-4 py-2 text-sm font-semibold text-white hover:bg-purple-500" %>
    </div>
  </div>
<% end %>

<div class="grid md:grid-cols-2 gap-8 max-w-4xl mx-auto">
  <!-- Quarterly Plan -->
  <div class="rounded-2xl border border-gray-200 bg-white p-8 shadow-sm">
    <h3 class="text-lg font-semibold text-gray-900">Quarterly License</h3>
    <p class="mt-4 flex items-baseline gap-x-2">
      <span class="text-5xl font-bold tracking-tight text-gray-900">$29</span>
      <span class="text-base text-gray-500">/quarter</span>
    </p>
    <p class="mt-6 text-base text-gray-600">Renews every 3 months. Perfect for trying out the software.</p>
    <ul class="mt-8 space-y-3 text-sm text-gray-600">
      <li class="flex gap-x-3">
        <svg class="h-5 w-5 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        Full software access
      </li>
      <li class="flex gap-x-3">
        <svg class="h-5 w-5 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        Quarterly billing cycle
      </li>
      <li class="flex gap-x-3">
        <svg class="h-5 w-5 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        Cancel anytime
      </li>
    </ul>
    <%= form_with url: checkout_path, method: :post, class: "mt-8" do |f| %>
      <%= f.hidden_field :price_id, value: @quarterly_price_id %>
      <%= f.submit "Subscribe Quarterly",
            class: "w-full rounded-md bg-indigo-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 cursor-pointer",
            disabled: @subscription&.active? %>
    <% end %>
  </div>

  <!-- Annual Plan -->
  <div class="rounded-2xl border-2 border-indigo-600 bg-white p-8 shadow-sm relative">
    <div class="absolute -top-4 left-1/2 -translate-x-1/2 rounded-full bg-indigo-600 px-4 py-1 text-sm font-semibold text-white">
      Best Value
    </div>
    <h3 class="text-lg font-semibold text-gray-900">Annual License</h3>
    <p class="mt-4 flex items-baseline gap-x-2">
      <span class="text-5xl font-bold tracking-tight text-gray-900">$99</span>
      <span class="text-base text-gray-500">/year</span>
    </p>
    <p class="mt-6 text-base text-gray-600">Save 15% compared to quarterly. Best for committed users.</p>
    <ul class="mt-8 space-y-3 text-sm text-gray-600">
      <li class="flex gap-x-3">
        <svg class="h-5 w-5 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        Full software access
      </li>
      <li class="flex gap-x-3">
        <svg class="h-5 w-5 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        Annual billing (save 15%)
      </li>
      <li class="flex gap-x-3">
        <svg class="h-5 w-5 text-indigo-600" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
        </svg>
        Cancel anytime
      </li>
    </ul>
    <%= form_with url: checkout_path, method: :post, class: "mt-8" do |f| %>
      <%= f.hidden_field :price_id, value: @annual_price_id %>
      <%= f.submit "Subscribe Annually",
            class: "w-full rounded-md bg-indigo-600 px-3.5 py-2.5 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 cursor-pointer",
            disabled: @subscription&.active? %>
    <% end %>
  </div>
</div>

<!-- Educational callout -->
<div class="mt-12 rounded-lg bg-indigo-50 border border-indigo-200 p-6 max-w-4xl mx-auto">
  <h3 class="text-lg font-semibold text-indigo-900">How this works</h3>
  <div class="mt-3 text-sm text-indigo-800 space-y-2">
    <p><strong>1. Subscribe:</strong> Clicking a button creates a Stripe Checkout Session via <code class="bg-indigo-100 px-1 rounded">payment_processor.checkout()</code></p>
    <p><strong>2. Stripe Checkout:</strong> You're redirected to Stripe's hosted page (use test card <code class="bg-indigo-100 px-1 rounded">4242 4242 4242 4242</code>)</p>
    <p><strong>3. Success redirect:</strong> Back to our app, which calls <code class="bg-indigo-100 px-1 rounded">Pay::Stripe.sync_checkout_session</code></p>
    <p><strong>4. Webhooks arrive:</strong> <code class="bg-indigo-100 px-1 rounded">checkout.session.completed</code>, <code class="bg-indigo-100 px-1 rounded">charge.succeeded</code>, <code class="bg-indigo-100 px-1 rounded">customer.subscription.created</code></p>
    <p><strong>5. Explore:</strong> Visit the <%= link_to "Explorer", explorer_path, class: "text-indigo-600 underline font-medium" %> to see every Pay table and webhook</p>
  </div>
</div>
