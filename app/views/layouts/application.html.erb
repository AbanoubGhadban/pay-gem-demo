<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title><%= content_for(:title) || "Pay Gem Demo" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= javascript_pack_tag "application", *Array(content_for(:packs)) %>
  </head>

  <body class="h-full">
    <nav class="bg-indigo-600">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="flex h-16 items-center justify-between">
          <div class="flex items-center">
            <%= link_to root_path, class: "flex items-center gap-2 text-white font-bold text-lg" do %>
              <svg class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="M2.25 8.25h19.5M2.25 9h19.5m-16.5 5.25h6m-6 2.25h3m-3.75 3h15a2.25 2.25 0 002.25-2.25V6.75A2.25 2.25 0 0019.5 4.5h-15a2.25 2.25 0 00-2.25 2.25v10.5A2.25 2.25 0 004.5 19.5z" />
              </svg>
              Pay Gem Demo
            <% end %>
            <div class="ml-10 flex items-baseline space-x-4">
              <%= link_to "Home", root_path, class: "rounded-md px-3 py-2 text-sm font-medium text-white hover:bg-indigo-500" %>
              <%= link_to "Account", account_path, class: "rounded-md px-3 py-2 text-sm font-medium text-white hover:bg-indigo-500" %>
              <%= link_to "Explorer", explorer_path, class: "rounded-md px-3 py-2 text-sm font-medium text-white hover:bg-indigo-500" %>
              <%= link_to "Lifecycle", lifecycle_path, class: "rounded-md px-3 py-2 text-sm font-medium text-white hover:bg-indigo-500" %>
            </div>
          </div>
          <div class="text-indigo-200 text-sm">
            <%= current_user&.email %>
          </div>
        </div>
      </div>
    </nav>

    <% if flash[:notice] %>
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
        <div class="rounded-md bg-green-50 p-4">
          <p class="text-sm font-medium text-green-800"><%= flash[:notice] %></p>
        </div>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 mt-4">
        <div class="rounded-md bg-red-50 p-4">
          <p class="text-sm font-medium text-red-800"><%= flash[:alert] %></p>
        </div>
      </div>
    <% end %>

    <main class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-8">
      <%= yield %>
    </main>
  </body>
</html>
